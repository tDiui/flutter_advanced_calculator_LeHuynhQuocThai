class CalculatorSettings {
  bool isDarkMode;
  int decimalPlaces;
  bool isDegreeMode;

  CalculatorSettings({
    this.isDarkMode = false,
    this.decimalPlaces = 4,
    this.isDegreeMode = true,
  });
}